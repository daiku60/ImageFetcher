# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ImageFetcher' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for ImageFetcher
  pod 'BNRDeferred', '~> 3.0'
  
  platform :ios, '8.0'
  pod 'SDWebImage', '~>3.8' 
 
target 'ImageFetcherTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ImageFetcherUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
